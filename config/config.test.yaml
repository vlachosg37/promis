alignment_files: tests/data/tiny.bam
input_dir: ""  # Directory scanned recursively for BAM/CRAM files when alignment_files is empty
reference_genome: ""  # Required for CRAM input; optional/empty for BAM input
container_image: "docker://ghcr.io/vlachosg37/promis:latest"  # Used only with container deployment

output_dir: output/tiny-promis
repeats: tests/data/tiny_loci.csv
cytoband: promis/workflow/database/cytoBand_hg38.txt
scripts_dir: promis/workflow/scripts

min_reads: 1
min_dev_reads: 1
msi_deviation: 0.1
bq_threshold: 30
mq_threshold: 60
call_by: both
min_dev_percent: 10.0
min_length_percent: 20.0
use_GMM: false
balance_tolerance: 0.05
min_total_reads: 1
